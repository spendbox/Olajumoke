Drop your background song here and name it:  track.mp3
(.m4a or .ogg also work — see MusicToggle.jsx for the source order)

The music button only appears/works once a real audio file is present.
Keep it small-ish (a few MB) so the page loads fast.
